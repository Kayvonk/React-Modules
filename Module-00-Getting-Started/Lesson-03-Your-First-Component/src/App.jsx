// LESSON: Your First Component
// A React component is a function whose name starts with a capital letter.
// You use it like an HTML tag: <MenuItem /> instead of calling MenuItem().

// TODO 1: Create a MenuItem component below that accepts name and price as props
//         and returns a <li> that displays both values.

// TODO 2: Inside the App return, render three <MenuItem /> tags with different
//         name and price values — wrap them in a <ul>.

// TODO 3: Wrap the <ul> in a <div> with an <h1> heading like "Today's Menu"
//         so the whole App returns a single root element.

export default function App() {
  return (
    <p>Replace this with your menu!</p>
  );
}
